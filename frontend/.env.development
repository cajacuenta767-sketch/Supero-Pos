# Solo se aplica a `npm run dev`. Vite no lee este fichero al compilar, así que
# el modo demostración no puede colarse en un build de producción por descuido.
VITE_DEMO_MODE=true
VITE_API_URL=http://localhost:3000/api/v1
